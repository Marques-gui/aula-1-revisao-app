import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aula 1( revisao app)',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
